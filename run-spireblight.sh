# /bin/bash

# requires this to be ran at the root of the repository - including this script as it is probable that other steps may be taken (for configuration, checks, CD, etc.)

docker compose up --build -d
